# Terraform (AWS) — STUB (MP-01)
Modules: vpc, eks (IRSA), ecr, elasticache (Redis 7, noeviction, auth token), secretsmanager, iam.
Validated with `terraform init -backend=false && terraform validate`; not applied (cost).
