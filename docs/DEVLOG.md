# DEVLOG

One entry per step, newest at the bottom. Public counterpart of the private planning log.

| Date | Step | What | Verification |
|------|------|------|--------------|
| 2026-09-03 | MP-02 | Solution skeleton: 8 src projects, 3 test projects, Central Package Management with pinned versions, analyzers as errors, commented stubs for every planned file, deploy/docs placeholders. | `dotnet restore` OK on all 11 projects; `dotnet build` of the 5 class libraries + Unit/Regression tests: 0 warnings, 0 errors. Hosts compile from MP-06. |
| 2026-09-04 | MP-03 | Domain and battle engine: Result/DomainError, PlayerId/BattleId, Resources, CombatStats, Player aggregate, xoshiro256** + SplitMix64 + FNV-1a seed, BattleRules, BattleEngine (integer math, deterministic), TurnEvent/LootResult/BattleReport. Tests: boundaries, spec examples, scripted rolls, 2,000-battle property test, PRNG known-answer tests, architecture dependency rules, 10 golden reports. Test stack moved to Microsoft.Testing.Platform mode (`global.json`), coverlet dropped. | Domain build 0 warnings; `dotnet test --project`: UnitTests 72/72, RegressionTests 10/10 |
| 2026-09-04 | MP-04 | Application layer: `Caller`, 9 ports (player repository, battle queue with consumer-group semantics, idempotent ledger, report store, leaderboard, tokens, events, clock, ids), use cases (create/get player, submit/get/process battle, leaderboard), `BattleNarrator`, `BattleScheduler` (no overlap, no overtaking, bounded concurrency), options with start-up validation, OpenTelemetry instruments, DI. Contracts: request/response DTOs, `ApiProblem`, polymorphic `ArenaEvent`s. Tests: in-memory fakes mirroring the Redis contracts (`FakeWorld`), scheduler scenarios + random simulation, handler tests. Source-generated logging (`LoggerMessage`) adopted after CA1873. ADR-0015 added, ADR-0005 completed. | Build 0 warnings, format clean; UnitTests 103/103 |
| 2026-09-05 | MP-05 | Redis infrastructure: key schema, single multiplexer, embedded Lua (`create_player`, `apply_battle`, `mark_battle`), adapters for every port (stream consumer group with XAUTOCLAIM and DLQ, idempotent ledger, report store, leaderboard, pub/sub events, ULID ids), source-generated JSON, health check. Integration tests on a Testcontainers Redis 7 with a key prefix per test. ADR-0016 (non-blocking stream reads). | IntegrationTests (Redis part) green |
| 2026-09-05 | MP-06 | Hosts: ServiceDefaults (OpenTelemetry traces/metrics/logs, Prometheus, OTLP opt-in, health endpoints), API (JWT HS256 + API-key exchange, policies with authenticated fallback, Problem Details with errors[], rate limiting, security headers, OpenAPI + Scalar, endpoints), Worker (single-writer processing loop with scheduler, XACK after settlement, dead-letter after 5 deliveries, graceful shutdown, gauges), `scripts/smoke.sh`. API tests via WebApplicationFactory and an API + worker end-to-end test. | Solution build 0 warnings; UnitTests 103, Regression 10, Integration 33; live smoke OK |
| 2026-09-05 | MP-06b | MCP server with the official C# SDK: Streamable HTTP (`/mcp`, protected by X-Api-Key) or stdio; tools create_player, get_player, submit_battle, get_battle_report, play_battle, get_leaderboard, simulate_battle, estimate_win_chance; cached service token; docs/mcp.md. | Build clean; initialize handshake verified live |
| 2026-09-05 | docs | Software design document (`docs/architecture.md`), Redis data model, API reference with curl, MCP guide. | — |
