# DEVLOG

One entry per step, newest at the bottom. Public counterpart of the private planning log.

| Date | Step | What | Verification |
|------|------|------|--------------|
| 2026-09-03 | MP-02 | Solution skeleton: 8 src projects, 3 test projects, Central Package Management with pinned versions, analyzers as errors, commented stubs for every planned file, deploy/docs placeholders. | `dotnet restore` OK on all 11 projects; `dotnet build` of the 5 class libraries + Unit/Regression tests: 0 warnings, 0 errors. Hosts compile from MP-06. |
| 2026-09-04 | MP-03 | Domain and battle engine: Result/DomainError, PlayerId/BattleId, Resources, CombatStats, Player aggregate, xoshiro256** + SplitMix64 + FNV-1a seed, BattleRules, BattleEngine (integer math, deterministic), TurnEvent/LootResult/BattleReport. Tests: boundaries, spec examples, scripted rolls, 2,000-battle property test, PRNG known-answer tests, architecture dependency rules, 10 golden reports. Test stack moved to Microsoft.Testing.Platform mode (`global.json`), coverlet dropped. | Domain build 0 warnings; `dotnet test --project`: UnitTests 72/72, RegressionTests 10/10 |
| 2026-09-04 | MP-04 | Application layer: `Caller`, 9 ports (player repository, battle queue with consumer-group semantics, idempotent ledger, report store, leaderboard, tokens, events, clock, ids), use cases (create/get player, submit/get/process battle, leaderboard), `BattleNarrator`, `BattleScheduler` (no overlap, no overtaking, bounded concurrency), options with start-up validation, OpenTelemetry instruments, DI. Contracts: request/response DTOs, `ApiProblem`, polymorphic `ArenaEvent`s. Tests: in-memory fakes mirroring the Redis contracts (`FakeWorld`), scheduler scenarios + random simulation, handler tests. Source-generated logging (`LoggerMessage`) adopted after CA1873. ADR-0015 added, ADR-0005 completed. | Build 0 warnings, format clean; UnitTests 103/103 |
| 2026-09-05 | MP-05 | Redis infrastructure: key schema, single multiplexer, embedded Lua (`create_player`, `apply_battle`, `mark_battle`), adapters for every port (stream consumer group with XAUTOCLAIM and DLQ, idempotent ledger, report store, leaderboard, pub/sub events, ULID ids), source-generated JSON, health check. Integration tests on a Testcontainers Redis 7 with a key prefix per test. ADR-0016 (non-blocking stream reads). | IntegrationTests (Redis part) green |
| 2026-09-05 | MP-06 | Hosts: ServiceDefaults (OpenTelemetry traces/metrics/logs, Prometheus, OTLP opt-in, health endpoints), API (JWT HS256 + API-key exchange, policies with authenticated fallback, Problem Details with errors[], rate limiting, security headers, OpenAPI + Scalar, endpoints), Worker (single-writer processing loop with scheduler, XACK after settlement, dead-letter after 5 deliveries, graceful shutdown, gauges), `scripts/smoke.sh`. API tests via WebApplicationFactory and an API + worker end-to-end test. | Solution build 0 warnings; UnitTests 103, Regression 10, Integration 33; live smoke OK |
| 2026-09-05 | MP-06b | MCP server with the official C# SDK: Streamable HTTP (`/mcp`, protected by X-Api-Key) or stdio; tools create_player, get_player, submit_battle, get_battle_report, play_battle, get_leaderboard, simulate_battle, estimate_win_chance; cached service token; docs/mcp.md. | Build clean; initialize handshake verified live |
| 2026-09-05 | docs | Software design document (`docs/architecture.md`), Redis data model, API reference with curl, MCP guide. | — |
| 2026-09-05 | MP-09 | Containers and deployment: multi-stage Dockerfile (chiseled, non-root, three targets), Compose stack with Redis (AOF, noeviction), API, worker, MCP and grafana/otel-lgtm with a provisioned RED/USE/economy dashboard; Helm chart (api Deployment/Service/PDB/HPA/Ingress, worker Recreate with 45 s grace, mcp, embedded Redis StatefulSet or external, Secret/existingSecret, NetworkPolicies, optional ServiceMonitor + SLO PrometheusRule); `helm-up.sh` / `k3d-up.sh` with explicit kube contexts; docs/deploy.md and docs/local-kubernetes.md. Debugging note: a first Compose smoke "failed" because stale `dotnet.exe` host processes from an earlier manual run still owned 127.0.0.1:8080-8082; killed them, everything green. | Compose: build 266 s, smoke OK, metrics via OTLP, dashboard OK · Helm on docker-desktop: lint clean, 4/4 pods, smoke OK, 49 s |
| 2026-09-05 | MP-07 | Live events: `ArenaHub` (auto-join own group from the token, back-office / watch for service tokens), `ArenaEventRelay` (Redis pub/sub → SignalR groups on every replica, no backplane; ADR-0010 rewritten), JWT via `access_token` query for hubs, worker publishes turn events (≤ 100 turns), outcome and leaderboard snapshot after settlement, `GET /players` for opponent discovery, arena auto-play client (two tabs = two players, animated HP bars). Tests: hub end to end through worker + Redis, anonymous rejected, list players, event ordering. | Build 0 warnings; Unit 107, Regression 10, Integration 35; hosts from source: /arena/ served, smoke OK, negotiate 200/401 |
| 2026-09-05 | MP-10 | GitHub Actions: `ci.yml` (build with warnings as errors, format check, unit + golden tests; integration tests against a `redis:7-alpine` service container; api/worker/mcp images with GHA layer cache and Trivy gate on HIGH/CRITICAL; helm lint + kubeconform) and `release.yml` on `v*` tags (GHCR images with SBOM and provenance, NuGet packages for Domain and Contracts, Helm chart as OCI). Static clients moved into `src/Coliseum.Api/wwwroot` (MSBuild links did not serve in Development). Repo published at github.com/atorrellascz/coliseum. | Local: build 0 warnings, Unit 107, Regression 10, Integration 35; CI run: see badge |
| 2026-09-05 | MP-09 (k3d) / hygiene | k3d v5.9.0 installed; `scripts/k3d-up.sh` measured (cluster 116 s, helm 49 s, smoke OK on 3 nodes; numbers in docs/local-kubernetes.md). Hosts' `Program.cs` reduced to composition-only scripts with `HostingExtensions` per host, enforced by `HostCompositionRulesTests` (ARQ-01). | Unit 110, Regression 10, Integration 35 |
| 2026-09-05 | MP-12 | Final documentation: README rewritten as the deliverable (quick start, curl walkthrough, architecture, decisions with ADR links, rules as implemented, SUP table, trade-offs, what was left out, next steps, quality gates, "What you can try"); every ADR completed; docs/security.md, docs/sre.md, six runbooks; `scripts/replay-dlq.sh`; AGENTS.md rewritten. | Docs only; CI unaffected |
| 2026-09-05 | MP-08 | Back-office and widget: `IGameStats` port with a Redis counters adapter (pipelined HINCRBY after each settlement), `GET /admin/stats` (service token) with economy, queue USE and top 10; back-office page (API-key sign-in, RED computed client-side from /metrics histograms, USE and economy panels with Chart.js, live feed via the hub with click-to-open reports); embeddable widget (shadow DOM, least-privilege player token, docs/widget.md); arena polish (floating damage, crits, misses, toasts, winner/loser highlight, seed with MCP replay hint, spectator mode, keyboard shortcuts, optional sound); CSP header for the static clients. | Unit 112, Integration 36; live check of every static asset, CSP presence, admin stats 401/403/200, smoke OK |
