# ADR-0006: One worker gives strict global order; partition to scale

- Status: Proposed
- Date: 2026-09-03

## Context and decision
With N replicas order is strict per worker only. Scaling path: partition the stream by attacker hash.

## Consequences
_To be completed when the micro-project that implements it lands._
